def solve():
    for i in range(int(input())):
        n = int(input())
        arr =  list(map(int, input().split()))

        s = sum(arr)

        # total sum of b not equal to sum of s
        #  is it possible to make sum using n items and also a[i]!=b[i]
        
        # if n = 1 ans = 1
        # if len(arr) == let(set(arr))
        
        
if __name__ == "__main__":
    solve()